/// Permission strings as returned in UserEntity.permissions (GET /auth/me).
/// Scoped for now to the DPR create → submit → approval/review flow, plus
/// the Fuel & Energy Logs flow. Extend this file as more screens get
/// gated later.
class Permissions {
  const Permissions._();
  //need to assign
  // static const String toolCustodyManage = "";
}
